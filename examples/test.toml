[server]
host = "localhost"
port = 8080

[server.ssl]
enabled = true
cert_path = "/path/to/cert.pem"

[database]
name = "myapp"
connections = 10
timeout = 30

features = ["auth", "logging", "metrics"]
debug = false
